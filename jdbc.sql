create table education(
	seq number primary key,
	title varchar(30) not null,
	url varchar(30) not null);
	
create table kpop(
	seq number primary key,
	title varchar(30) not null,
	url varchar(30) not null);
	
create table travel(
	seq number primary key,
	title varchar(30) not null,
	url varchar(30) not null);